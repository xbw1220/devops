spring:
  cloud:
    nacos:
      discovery:
        server-addr: nacos:8848
        network-interface: eth0
      config:
        server-addr: nacos:8848
        file-extension: properties
    sentinel:
      eager: true
      transport:
        dashboard: sentinel:8858
  application:
    name: devops-client