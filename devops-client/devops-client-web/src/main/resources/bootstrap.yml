
spring:
  cloud:
    nacos:
      discovery:
        server-addr: nacos_server:8848
    inetutils:
      use-only-site-local-interfaces: true
      preferred-networks: ['10.0.9']
  application:
    name: devops-client