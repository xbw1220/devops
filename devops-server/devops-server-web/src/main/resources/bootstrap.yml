spring:
  cloud:
    nacos:
      discovery:
        server-addr: 10.0.9.250:8848
      config:
        server-addr: 10.0.9.250:8848
        file-extension: properties
    inetutils:
      use-only-site-local-interfaces: true
      preferred-networks: ['10.0.9']
  application:
    name: devops-server
server:
  port: 8081