name: devops Java CI

#on: [push]
on:
  push:
    branches:
      - master

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 1.8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8

      #        mvn -q -e -B -U clean install -pl devops-client/devops-client-api
    - name: Build
      run: |
        mvn -q -e -B -U clean install -pl devops-client/devops-client-api
        mvn -q -e -B -U clean install package -Dmaven.test.skip=true -pl devops-client/devops-client-web
        mvn -q -e -B -U clean install -pl devops-server/devops-server-api
        mvn -q -e -B -U clean package -Dmaven.test.skip=true -pl devops-server/devops-server-web

    - name: Upload Artifact
      uses: actions/upload-artifact@v1
      with:
        name: Build
        path: "**/target/*.jar"
      

